# Server-only — read via process.env in src/lib/expenses.functions.ts,
# never exposed to the browser. Get these by deploying apps-script/Code.gs
# as a Web App (see the instructions in that file's header comment).
APPS_SCRIPT_URL=
APPS_SCRIPT_TOKEN=
